import React, {useState, useEffect} from 'react';
import { List, PostId } from 'DataManager/DataManager';
import {Grid, FormControl, InputLabel, Select, MenuItem, Input, Button} from '@material-ui/core';
const AddFlightAttachment = (props) => {
    return(
        <div>test</div>
    )
}
export default AddFlightAttachment;